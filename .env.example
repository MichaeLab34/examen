# FastAPI runtime
DECROCHAGE_MODEL_PATH=artifacts/models/model_bundle.joblib

# SQL persistence. Local default can be SQLite, production-like stack uses Postgres.
DECROCHAGE_DATABASE_URL=postgresql+psycopg://decrochage:change-me@localhost:5432/decrochage

# Required for RGPD pseudonymisation from Silver onward.
# Generate locally, keep secret, rotate through DPO-approved procedure.
DECROCHAGE_PSEUDONYMIZATION_SECRET=change-me-with-openssl-rand-hex-32

# Retention window for risk scores and medallion batches.
DECROCHAGE_RETENTION_DAYS=365

# Docker Compose Postgres settings.
POSTGRES_DB=decrochage
POSTGRES_USER=decrochage
POSTGRES_PASSWORD=change-me

# Optional. When set, clients must send this value as the X-API-Key header.
DECROCHAGE_API_KEY=

# Per-client application limit for prediction and model reload endpoints.
# A shared edge limiter remains required when scaling beyond one API instance.
DECROCHAGE_RATE_LIMIT_PER_MINUTE=60

# Model lifecycle: Docker exposes MLflow on localhost; containers use mlflow:5000.
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_ARTIFACT_ROOT=artifacts/mlflow-artifacts
MLFLOW_EXPERIMENT_NAME=decrochage-l1-training
# Set after the first candidate has been promoted to production.
DECROCHAGE_REGISTERED_MODEL=
DECROCHAGE_MODEL_ALIAS=production

# Restitution portal (/portal). Disabled by default: a pure inference deployment
# then exposes no authenticated web surface at all.
DECROCHAGE_PORTAL_ENABLED=false
# Required as soon as the portal is enabled; the service refuses to start without it.
# Generate locally (openssl rand -hex 32), keep secret, rotate through the DPO procedure.
DECROCHAGE_PORTAL_SECRET=change-me-with-openssl-rand-hex-32
DECROCHAGE_PORTAL_SESSION_HOURS=8
DECROCHAGE_PORTAL_PAGE_SIZE=50
# Anti-exfiltration ceiling on CSV exports handed to the information system.
DECROCHAGE_PORTAL_EXPORT_MAX_ROWS=1000
DECROCHAGE_PORTAL_LOGIN_MAX_ATTEMPTS=5
DECROCHAGE_PORTAL_LOGIN_WINDOW_MINUTES=15
# The session cookie carries `Secure` by default, so it never travels over plain
# HTTP. Set this to true ONLY to reach the portal on http://localhost without a
# TLS terminator; behind Caddy it must stay false.
DECROCHAGE_PORTAL_ALLOW_INSECURE_COOKIE=false

# Public HTTPS endpoint handled by Caddy in the `run` Compose profile.
DECROCHAGE_DOMAIN=localhost

# Optional dead-man's switch for scheduled scoring/monitoring jobs.
DECROCHAGE_HEALTHCHECK_URL=

# Scheduled monitoring and candidate retraining. Five-field cron expressions.
DECROCHAGE_REFERENCE_CSV=data/raw/decrochage_etudiants_complet_V5.csv
DECROCHAGE_CURRENT_CSV=data/raw/decrochage_etudiants_echantillon_V5.csv
DECROCHAGE_STUDENTS_CSV=data/raw/decrochage_etudiants_complet_V5.csv
DECROCHAGE_CATALOGUE_CSV=data/raw/catalogue_formations_V5.csv
DECROCHAGE_DRIFT_REPORT_PATH=reports/drift_report.json
DECROCHAGE_SCHEDULER_STATE=artifacts/scheduler/state.json
DECROCHAGE_TRAINED_ON=2024-09-01
DECROCHAGE_LABELS_AVAILABLE=false
DECROCHAGE_PERFORMANCE_ALERT=false
DECROCHAGE_MONITOR_CRON=0 6 * * 1
DECROCHAGE_RETRAIN_CRON=0 7 * * 1
DECROCHAGE_SCHEDULER_TIMEZONE=Europe/Paris

# Optional Grafana contact point (Teams/Slack-compatible webhook selected by the DSI).
GRAFANA_ADMIN_PASSWORD=change-me
DECROCHAGE_ALERT_WEBHOOK_URL=http://localhost:9999/alert-placeholder
