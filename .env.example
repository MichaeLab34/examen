# FastAPI runtime
DECROCHAGE_MODEL_PATH=artifacts/models/model_bundle.joblib

# SQL persistence. Default local SQLite database used by the medallion pipeline.
DECROCHAGE_DATABASE_URL=sqlite:///artifacts/decrochage.db

# Optional. When set, clients must send this value as the X-API-Key header.
DECROCHAGE_API_KEY=
